--select count(*) from customers where age>22;
--select lower(first_name) as first_name from customers;
--select substring(first_name,1,3) as firstname from customers;
--SELECT SUBSTRING(country, 0, CHARINDEX('A',country)) FROM customers;
--create table newtab(id,name,name1,age,country);
--insert into newtab select * from customers;
--CREATE TABLE NewTable AS SELECT * FROM orders;
--select age, count(*) as total_item from customers where age=22 group by age;
--insert into orders values(6,"k",400,2);
--insert into orders values(7,"k",400,1);
--insert into orders values(8,"k",400,3);
--insert into orders values(9,"k",400,1);
--select customer_id, count(*) as total_item from orders where item="keyboard" group by customer_id;
--select * from orders where amount>400 order by amount asc;


find the number of customers in each country". The GROUP BY statement is often used with aggregate functions ( COUNT() , MAX() , MIN() , SUM() , AVG() ) to group the result-set by one or more columns.
